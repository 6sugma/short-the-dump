# Local dashboard mutation guard. Use a long random value and do not commit the real token.
SHORT_THE_DUMP_OPERATOR_TOKEN=replace-me

# Only considered with mode=limited_live, live_order_routing_enabled=true,
# an explicitly supplied live router, and completed live-readiness review.
# SHORT_THE_DUMP_LIVE_ACK=I_UNDERSTAND_LIVE_SHORT_RISK

# SEC requires an organization/contact identity from deployment configuration.
# SEC_USER_AGENT=Research Team research@example.com
